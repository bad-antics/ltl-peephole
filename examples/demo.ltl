// examples/demo.ltl — runnable demo for ltl_peephole

import std::io
use ltl_peephole as lib

fn main() {
    io::println("ltl_peephole demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}