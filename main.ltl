// ltl-peephole/main.ltl
//
// Peephole optimiser demos over a 3-address IR.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-peephole v0.1.0")
    io::println("Peephole optimiser demos over a 3-address IR.")
    0
}
